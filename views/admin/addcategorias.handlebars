<!--Arquivo para desenvolvimento da pagina de formulario. Onde serao adicionadas novas categorias-->
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h4>Criar nova categoria:</h4>
<div class="card">
    <div class="card-body">
    <form action="/admin/categorias/nova" method="POST">
        <label for="nome">Nome:</label>
        <input type="text" name="nome" id="nome" placeholder="Nome da Categoria" class="form-control">
        <label for="nome">Slug:</label>
        <input type="text" name="slug" id="slug" placeholder="Slug/Hashtag da Categoria" class="form-control">
        <br>
        <button type="submit" class="btn btn-success">Criar</button>
    </form>
</div>
</div>